# Итоговый отчёт: Расследование Layer 4

**Дата**: 2026-03-01
**Math Consultant**: Galois Theory Expert

---

## 📋 Краткое резюме

### ✅ Что проверили:
1. **Логику кода** — как проверяется нормальность подгрупп
2. **Данные** — корректность всех 24 уровней Layer 4
3. **Игровую механику** — можно ли обмануть систему

### 🔍 Главные выводы:
1. **Код математически корректен** ✅
2. **Level 21 имеет ошибки в данных** ❌
3. **Игровую механику можно обмануть** через выбор h=e 🚨

---

## 1. Проверка кода

### Вопрос пользователя:
> "Посмотри по коду, как доказывается что группа ненормальная?"

### Ответ:

Код использует **два уровня проверки**:

#### A. Runtime проверка (игровая механика)
**Файл**: `conjugation_cracking_manager.gd:131-184`

```gdscript
func test_conjugation(g_sym_id: String, h_sym_id: String) -> Dictionary:
    # Вычисляем g · h · g⁻¹
    var conjugate: Permutation = g_perm.compose(h_perm).compose(g_inv)

    # Проверяем: в подгруппе ли результат?
    var stayed_in: bool = elements.has(result_sym_id)

    # Если НЕ в подгруппе → найден witness → НЕнормальна!
    var is_witness: bool = not stayed_in

    if is_witness:
        _classified[index] = {"is_normal": false}  // ✅ Правильно!
```

**Логика**: Если `g·h·g⁻¹ ∉ H` → подгруппа НЕнормальна ✅

#### B. Математическая валидация
**Файл**: `subgroup_checker.gd:8-23`

```gdscript
static func is_normal(subgroup: Array, group: Array) -> bool:
    for g in group:
        for h in subgroup:
            var conjugate: Permutation = g.compose(h).compose(g_inv)

            # Если хоть один conjugate НЕ в подгруппе → НЕнормальна!
            if conjugate not in subgroup:
                return false  // ✅ Правильно!

    return true  // Все сопряжённые в H → нормальна
```

**Логика**: Перебирает ВСЕ пары (g, h) и проверяет `g·h·g⁻¹ ∈ H` ✅

### ✅ Вывод: Код корректен!

Условия **НЕ перепутаны**:
- `stayed_in = false` → `is_witness = true` → `is_normal = false` ✅
- `not found` → `return false` (НЕнормальна) ✅

---

## 2. Проверка данных

### Скрипт: `check_layer4_logic.py`

Результаты:
- **23 уровня**: ✅ Все данные корректны
- **1 уровень (Level 21)**: ❌ 4 ошибки

### Ошибки в Level 21 (Q8 — кватернионы):

```
Level 21: 5 subgroups - 4 ISSUES!
  Subgroup {id, neg, i, ni} (order 4):
    ❌ is_normal=False but no witness!

  Subgroup {id, neg, j, nj} (order 4):
    ❌ is_normal=False but no witness!

  Subgroup {id, neg, i, ni} (order 4):
    ❌ witness computation wrong!
    Expected: [6,7,4,5,3,2,0,1]
    Got:      [7,6,5,4,3,2,0,1]

  Subgroup {id, neg, i, ni} (order 4):
    ❌ witness computation wrong!
    Expected: [7,6,5,4,2,3,1,0]
    Got:      [6,7,4,5,2,3,1,0]
```

### Математический факт:
**Q8 (группа кватернионов)** имеет **ВСЕ подгруппы нормальные**!

Это уникальное свойство Q8 — единственная некоммутативная группа порядка 8 с таким свойством.

### 📝 Исправление:
Все подгруппы Level 21 должны иметь:
```json
{
  "is_normal": true,
  "conjugation_witness": null
}
```

---

## 3. 🚨 КРИТИЧЕСКАЯ ОШИБКА в игровой механике

### Проблема:
Пользователь сообщил:
> "Я для всех случаев брал h = e в итоге мы оставались в группе и это проходило как решение."

### Что происходит:

#### Шаг 1: Игрок выбирает h = e (identity)
```
g · e · g⁻¹ = g · g⁻¹ = e
```

#### Шаг 2: Результат ВСЕГДА в подгруппе
Потому что **e ∈ H** для любой подгруппы H!

#### Шаг 3: Игра показывает "∈ H ✓"
Игрок думает: "Подгруппа нормальная!"

### Почему это проблема?

**Математически**: h ДОЛЖЕН выбираться из активной подгруппы H!

**Определение**:
```
H ⊴ G  ⟺  ∀g ∈ G, ∀h ∈ H: g·h·g⁻¹ ∈ H
            ^^^^^^^^
            h из подгруппы H, а НЕ из всей группы G!
```

### Текущее состояние кода:

**Файл**: `cracking_panel.gd:692-703`

```gdscript
func set_h(sym_id: String) -> void:
    if _cracking_mgr == null:
        return

    _h_sym_id = sym_id  // ❌ НЕТ проверки: sym_id ∈ H?
    _update_slot_display(_h_slot, sym_id, true)

    _set_active_input("g")
    g_slot_tapped.emit()
    _update_crack_btn_visibility()
```

**Проблема**: Игрок может выбрать **ЛЮБОЙ элемент графа**, а не только из подгруппы H!

### ✅ Но есть защита!

Когда игрок нажимает кнопку "⚑ Нормальная", код вызывает:

```gdscript
func confirm_normal() -> Dictionary:
    // Проверяет через SubgroupChecker.is_normal() — полный перебор!
    var is_actually_normal: bool = SubgroupChecker.is_normal(sub_perms, group_perms)

    var correct: bool = is_actually_normal

    if correct:
        _classified[index] = {"is_normal": true}  // ✅ Только если ПРАВДА нормальна!

    return {"confirmed": correct}
```

**Защита работает!** Даже если игрок "обманывает" через h=e, кнопка "Нормальная" всё равно проверит математику.

### 🎓 Но педагогически — провал!

Игрок **не понимает** математику, а просто **угадывает**:
- "Попробую h=e... показывает ∈ H... нажму 'Нормальная'... не сработало... значит НЕнормальна"

**Потеря обучающего эффекта**:
- Не видит witness (конкретный пример выхода из подгруппы)
- Не исследует структуру группы
- Не понимает сопряжение

---

## 4. Проверка данных: h ∈ H?

### Скрипт: `check_layer4_h_in_subgroup.py`

Результат:
```
Checked 63 witnesses across all levels.

✅ All witnesses have h ∈ H
   Data is mathematically correct!
```

**Вывод**: Все witness в JSON корректны — h всегда принадлежит подгруппе H ✅

---

## 📝 Рекомендации

### 🔴 КРИТИЧНО — исправить до релиза:

#### 1. Ограничить выбор h только элементами подгруппы

**Файл**: `cracking_panel.gd:692`

```gdscript
func set_h(sym_id: String) -> void:
    if _cracking_mgr == null:
        return

    // ✅ НОВАЯ ПРОВЕРКА:
    var active_idx: int = _cracking_mgr.get_active_subgroup_index()
    if active_idx < 0:
        return  # Нет активной подгруппы

    var subgroup_elements: Array = _cracking_mgr.get_subgroup_elements(active_idx)
    if not subgroup_elements.has(sym_id):
        // ❌ Выбран элемент ВНЕ подгруппы
        _show_error_message("h должен быть из брелка H!")
        return

    _h_sym_id = sym_id
    _update_slot_display(_h_slot, sym_id, true)
    // ... остальной код
```

#### 2. Добавить визуальную подсказку

Когда h-slot активен:
- **Подсветить зелёным** ⊕-кнопки, которые в подгруппе H
- **Затемнить серым** остальные кнопки

#### 3. Исправить Level 21

Все подгруппы Q8 должны иметь:
```json
{
  "is_normal": true,
  "conjugation_witness": null
}
```

### ⚠ Важно:

#### 4. Добавить туториал в первом уровне Layer 4

Объяснить:
- "h — элемент из брелка (подгруппы), который мы тестируем"
- "g — любой элемент группы, который мы используем для 'вращения'"

#### 5. Показать элементы H рядом с h-slot

Например:
```
h — элемент брелка
Доступные: {e, r2}
```

---

## 📊 Сводка

| Аспект | Статус | Действие |
|--------|--------|----------|
| **Код логики** | ✅ Корректен | Нет действий |
| **Код защиты** | ✅ Работает | Нет действий |
| **Level 21 данные** | ❌ Ошибки | Исправить JSON |
| **Остальные уровни** | ✅ Корректны | Нет действий |
| **Выбор h** | 🚨 КРИТИЧНО | Добавить проверку |
| **UI подсказки** | ⚠ Отсутствуют | Добавить подсветку |
| **Туториал** | ⚠ Неполный | Расширить объяснение |

---

## 🎯 Приоритеты

### 🔴 Критично (блокирует релиз):
1. Добавить проверку `h ∈ H` в `set_h()`
2. Исправить Level 21 JSON

### 🟡 Важно (улучшает UX):
3. Визуальная подсветка валидных h
4. Сообщение об ошибке при неверном h
5. Туториал Layer 4

### 🟢 Желательно (polish):
6. Показать список элементов H рядом с h-slot
7. Заблокировать кнопки вне H в h-режиме

---

## Файлы отчётов

1. **`LAYER4_IMPLEMENTATION_ANALYSIS.md`** — подробный разбор кода
2. **`CRITICAL_BUG_LAYER4_H_SELECTION.md`** — описание критической ошибки
3. **`check_layer4_h_in_subgroup.py`** — скрипт проверки данных
4. **`SUMMARY_LAYER4_INVESTIGATION.md`** — этот документ

---

**Дата**: 2026-03-01
**Math Consultant**: Galois Theory Expert
**Статус**: ✅ Расследование завершено
