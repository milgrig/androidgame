# Layer 4 UX Problems — Отчёт пользователя

**Дата**: 2026-03-01
**Источник**: Feedback от пользователя
**Math Consultant**: Galois Theory Expert

---

## 🗣️ Цитата пользователя

> "Я для всех случаев брал h = e в итоге мы оставались в группе и это проходило как решение."

> "Но для ненормальной группы мы же должны выйти из H, а не остаться. Так ведь?"

> "Во время игры, я выбираю вместо h - e, любой g. Разумеется после этого я попадаю в e, остаюсь в H. Это ничего не говорит о нормальности группы. Но при этом происходит сообщение что одного этого примера достаточно, чтоб понять нормальная группа или нет."

---

## 🎯 Суть проблемы

Пользователь заметил **три критические проблемы UX**:

### 1. h можно выбрать из любого элемента графа
- **Эксплойт**: Всегда выбирать h = e (identity)
- **Результат**: `g · e · g⁻¹ = e ∈ H` (всегда!)
- **Последствие**: Зелёное сообщение "∈ H ✓" вводит в заблуждение

### 2. Зелёное "✓" создаёт ложное впечатление
- **UI показывает**: `"= e ∈ H  ✓"` (зелёный цвет)
- **Игрок думает**: "Галочка ✓ → всё правильно → нормальная!"
- **Правда**: Один тест ∈ H НЕ доказывает нормальность!

### 3. Кнопка "Нормальная" доступна сразу
- **По дизайну** (redesign.md:220): "активна после минимум 10 попыток"
- **Сейчас**: Видна сразу при выборе подгруппы
- **Последствие**: Игрок может угадывать, не исследуя

---

## 🔍 Анализ кода

### Проблема 1: Нет проверки h ∈ H

**Файл**: `cracking_panel.gd:692-703`

```gdscript
func set_h(sym_id: String) -> void:
    if _cracking_mgr == null:
        return

    _h_sym_id = sym_id  // ❌ НЕТ проверки: sym_id ∈ H?
    _update_slot_display(_h_slot, sym_id, true)

    _set_active_input("g")
    g_slot_tapped.emit()
    _update_crack_btn_visibility()
```

**Что происходит**:
- Игрок может выбрать **любой** элемент графа (любую ⊕-кнопку)
- **h = e** всегда даёт `e ∈ H` (зелёная галочка)
- Игрок не понимает, что нужно тестировать **разные h из подгруппы**

---

### Проблема 2: UI сообщения вводят в заблуждение

**Файл**: `cracking_panel.gd:723-728`

```gdscript
if stayed_in:
    _result_label.text = "= %s ∈ H  ✓" % r_name  // ❌ Зелёная галочка!
    _result_label.add_theme_color_override("font_color", L4_GREEN)
else:
    _result_label.text = "= %s ∉ H  — ВЗЛОМ!" % r_name
    _result_label.add_theme_color_override("font_color", L4_RED)
```

**Проблема**:
- **Зелёная галочка ✓** ассоциируется с "правильно, готово"
- **Зелёный цвет** = успех, завершение
- Игрок думает: "Всё ок, подгруппа нормальная!"

**Дизайн из redesign.md** (строка 195-196):
> - Если результат совпадает с ключом из брелка → этот ключ **вспыхивает внутри брелка** (зелёное свечение)
> - Если результат НЕ совпадает → этот ключ **вспыхивает снаружи брелка** (красное свечение + звук треска)

**Интерпретация**:
- Зелёный = "результат в брелке" (факт, не оценка)
- Красный = "результат вне брелка — ВЗЛОМ!" (найдена дыра)

**НО**: Текст "✓" (галочка) придаёт **оценочный** характер зелёному цвету!

---

### Проблема 3: Кнопка "Нормальная" показывается сразу

**Файл**: `cracking_panel.gd:754`

```gdscript
_confirm_btn.visible = (_cracking_mgr.get_active_subgroup_index() >= 0)
```

**Логика**: Кнопка видна, если выбрана подгруппа (сразу!)

**Дизайн из redesign.md** (строка 220-221):
> **Появляется кнопка** (активна после минимум 10 попыток):
> **«Этот брелок невозможно взломать»**

**Должно быть**:
```gdscript
var min_attempts: int = 10
var attempts: int = _test_history.size()
_confirm_btn.visible = (attempts >= min_attempts)
```

---

## 🎓 Педагогическая проблема

### Что игрок должен понять:
1. **Нормальная подгруппа**: `∀g ∈ G, ∀h ∈ H: g·h·g⁻¹ ∈ H`
2. **НЕнормальная**: `∃g, h: g·h·g⁻¹ ∉ H` (хоть одна пара!)
3. **Witness** — конкретная пара (g, h), где выход из H
4. **Исследование**: Нужно пробовать разные h из подгруппы

### Что игрок понимает сейчас:
1. "Выбираю h = e → зелёная галочка → нормальная?"
2. "Зелёная галочка ✓ = успех, всё правильно"
3. "Красный = ошибка, что-то не так"
4. **ПОТЕРЯ СМЫСЛА**: Игрок не исследует структуру группы!

---

## 📊 Сравнение: Дизайн vs Реализация

| Аспект | Дизайн (redesign.md) | Реализация | Статус |
|--------|---------------------|-----------|--------|
| **h из подгруппы** | h выбирается с брелка | h — любой элемент | ❌ Не реализовано |
| **Зелёный цвет** | Вспышка ключа в брелке | "∈ H ✓" | ⚠️ Частично |
| **Красный цвет** | Вспышка + треск | "∉ H — ВЗЛОМ!" | ✅ Корректно |
| **Кнопка "Нормальная"** | После ≥10 попыток | Сразу | ❌ Не реализовано |
| **Счётчик попыток** | Видимый (5, 10, 15...) | Невидимый | ❌ Отсутствует |

---

## 📝 Рекомендации по исправлению

### 🔴 КРИТИЧНО — исправить до релиза

#### 1. Ограничить выбор h элементами подгруппы

**Файл**: `cracking_panel.gd:692`

```gdscript
func set_h(sym_id: String) -> void:
    if _cracking_mgr == null:
        return

    // ✅ ДОБАВИТЬ:
    var active_idx: int = _cracking_mgr.get_active_subgroup_index()
    if active_idx < 0:
        return

    var subgroup_elements: Array = _cracking_mgr.get_subgroup_elements(active_idx)
    if not subgroup_elements.has(sym_id):
        // ❌ Элемент ВНЕ подгруппы
        _show_error_message("h должен быть из брелка H!")
        return

    _h_sym_id = sym_id
    // ... остальной код
```

#### 2. Изменить UI сообщения (убрать галочку ✓)

**Файл**: `cracking_panel.gd:723-728`

**Вариант A** (нейтральный):
```gdscript
if stayed_in:
    _result_label.text = "= %s ∈ H (остался в брелке)" % r_name
    _result_label.add_theme_color_override("font_color", L4_GOLD)  // Жёлтый
else:
    _result_label.text = "= %s ∉ H — ВЗЛОМ!" % r_name
    _result_label.add_theme_color_override("font_color", L4_RED)
```

**Вариант B** (с подсказкой):
```gdscript
if stayed_in:
    _result_label.text = "= %s ∈ H (продолжайте искать)" % r_name
    _result_label.add_theme_color_override("font_color", L4_GOLD)
else:
    _result_label.text = "= %s ∉ H — НЕнормальна!" % r_name
    _result_label.add_theme_color_override("font_color", L4_RED)
```

**Вариант C** (со счётчиком):
```gdscript
if stayed_in:
    var n = _test_history.size()
    _result_label.text = "= %s ∈ H (тест %d)" % [r_name, n]
    _result_label.add_theme_color_override("font_color", L4_GOLD)
else:
    _result_label.text = "= %s ∉ H — ВЗЛОМ!" % r_name
    _result_label.add_theme_color_override("font_color", L4_RED)
```

#### 3. Кнопка "Нормальная" только после ≥10 попыток

**Файл**: `cracking_panel.gd:754`

```gdscript
// Старая логика:
// _confirm_btn.visible = (_cracking_mgr.get_active_subgroup_index() >= 0)

// ✅ НОВАЯ ЛОГИКА:
if _confirm_btn and _cracking_mgr:
    var active_idx = _cracking_mgr.get_active_subgroup_index()
    var attempts = _test_history.size()
    var min_attempts = 10

    if active_idx >= 0 and attempts >= min_attempts:
        _confirm_btn.visible = true
        _confirm_btn.text = "⚑ Невозможно взломать (%d попыток)" % attempts
    else:
        _confirm_btn.visible = false
```

---

### 🟡 ВАЖНО — улучшает UX

#### 4. Подсветить валидные h-кнопки

Когда h-slot активен:
- **Зелёная рамка** вокруг ⊕-кнопок из подгруппы H
- **Затемнить** остальные кнопки (или показать 🚫)

#### 5. Показать счётчик попыток

Добавить label под maneuver zone:
```
Попытки: 7/10 (ещё 3 до возможности подтвердить)
```

#### 6. Добавить подсказку для h-slot

```
h — элемент брелка
Доступные: {e, r2, r4, r6}
```

---

### 🟢 ЖЕЛАТЕЛЬНО — polish

#### 7. Анимация вспышки ключа

Реализовать дизайн из redesign.md:
- Результат ∈ H → **вспышка ключа внутри брелка** (слева)
- Результат ∉ H → **вспышка ключа снаружи** (справа) + звук треска

#### 8. Туториал в первом Layer 4 уровне

Объяснить:
1. "h выбирается ИЗ брелка"
2. "g — любой ключ (может быть вне брелка)"
3. "Попробуй разные h, чтобы найти дыру"
4. "Если не можешь найти дыру — после 10 попыток можешь подтвердить нормальность"

---

## 🧪 Тестирование после исправления

### Тест 1: Попытка выбрать h ∉ H
1. Выбрать подгруппу H = {e, r2}
2. Активировать h-slot
3. Нажать ⊕ на элементе s ∉ H
4. **Ожидается**: Сообщение "h должен быть из брелка H!"

### Тест 2: Зелёное сообщение без галочки
1. Выбрать h = e, g = r1
2. Получить результат e ∈ H
3. **Ожидается**: Жёлтое (не зелёное!) сообщение "∈ H (остался в брелке)"
4. **НЕ должно быть**: Галочка ✓ или "успех"

### Тест 3: Кнопка после 10 попыток
1. Выбрать нормальную подгруппу
2. Сделать 5 попыток
3. **Ожидается**: Кнопка "Нормальная" НЕ видна
4. Сделать ещё 5 попыток (всего 10)
5. **Ожидается**: Кнопка появляется с текстом "(10 попыток)"

### Тест 4: Эксплойт через h=e больше не работает
1. Выбрать НЕнормальную подгруппу
2. Попытаться всегда выбирать h=e
3. **Ожидается**: Можно выбрать h=e (оно в подгруппе)
4. Результат всегда "∈ H (остался в брелке)"
5. После 10 попыток нажать "Нормальная"
6. **Ожидается**: Кнопка НЕ срабатывает (защита SubgroupChecker)
7. Сообщение: "Эта подгруппа НЕ нормальна! Попробуй другие h."

---

## 📊 Итоговая таблица проблем

| # | Проблема | Severity | Файл | Строка | Статус |
|---|----------|----------|------|--------|--------|
| 1 | h из любого элемента G | 🔴 CRITICAL | cracking_panel.gd | 692 | ❌ Требует исправления |
| 2 | Зелёная галочка ✓ вводит в заблуждение | 🟡 HIGH | cracking_panel.gd | 724 | ❌ Требует исправления |
| 3 | Кнопка "Нормальная" сразу | 🔴 CRITICAL | cracking_panel.gd | 754 | ❌ Требует исправления |
| 4 | Нет счётчика попыток | 🟡 MEDIUM | - | - | ❌ Отсутствует |
| 5 | Нет подсветки валидных h | 🟢 LOW | - | - | ❌ Отсутствует |

---

## 🎯 Влияние на обучение

### До исправления:
- ❌ Игрок не понимает математику
- ❌ Превращается в "угадайку"
- ❌ Эксплойт через h=e
- ❌ Нет исследования структуры

### После исправления:
- ✅ Игрок исследует подгруппу
- ✅ Понимает, что такое witness
- ✅ Видит разницу между нормальными и ненормальными
- ✅ Обучается через эксперимент

---

**Дата**: 2026-03-01
**Автор**: Math Consultant
**Приоритет**: 🔴 КРИТИЧЕСКИЙ — блокирует релиз
**Ответственные**: Developer (Game) + Developer (UI)
