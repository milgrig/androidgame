# Task discussion T101: Level generator: extend generate_complex_levels.py into a reusable tool

## [2026-02-27 22:52] boss (author)

### Task description

CONTEXT: generate_complex_levels.py exists in project root. It can create levels with S_n, A_n, D_n, Z_n groups on tetrahedron, cube, octahedron, complete graph structures.

TASK:
1. Review existing generate_complex_levels.py
2. Extend it into a proper CLI tool:
   - python generate_complex_levels.py --group D5 --graph cycle_5 --level-id 25
   - python generate_complex_levels.py --group S3 --graph complete_3 --level-id 26
   - python generate_complex_levels.py --list-groups (show available groups)
   - python generate_complex_levels.py --list-graphs (show available graph structures)
3. Add more graph structures:
   - Cycle graphs (C_n)
   - Path graphs (P_n)
   - Bipartite graphs (K_{m,n})
   - Prism graphs
   - Wheel graphs
4. Add auto-computation of automorphism group:
   - Given a graph, compute its Aut(G) automatically
   - Generate Cayley table
   - Find all subgroups (for Layer 3 data)
5. Validate generated levels:
   - Check that automorphisms are correct
   - Check that graph is valid (connected, proper colors/edges)
   - Output warnings for levels with too many/few automorphisms
6. Output format must match existing level JSON schema (see data/levels/act1/level_01.json)

VERIFICATION:
- CLI tool runs and generates valid JSON levels
- Generated levels load in the game (unit test)
- At least 3 different graph types work
- Automorphism computation is correct (verified by math tests)

### Acceptance criteria

[To be clarified]

---
