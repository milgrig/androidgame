# Task discussion T136: Layer 4 — нормальные подгруппы через Room Clusters

## [2026-03-03 12:54] boss (author)

### Task description

Заменить вызовы highlight_subgroup() на set_room_clusters() в layer_mode_controller.gd для Layer 4.

ЧТО НУЖНО: При выборе подгруппы для "взлома" → красный кластер:
```gdscript
room_map.set_room_clusters([{
    "rooms": sym_ids_to_room_indices(elements),
    "color": Color(0.9, 0.35, 0.3, 1.0),  # L4_RED
    "label": "N",
    "show_internal_edges": true,
}])
```

При подтверждении нормальности → кластер может стать зелёным.
При "взломе" (ненормальная) → кластер красный с другим оттенком.

ФАЙЛ: src/game/layer_mode_controller.gd — секция Layer 4, вызовы highlight_subgroup.

### Acceptance criteria

- [ ] При выборе подгруппы на Layer 4 — красный кластер на карте
- [ ] При переключении между подгруппами — кластер обновляется
- [ ] При выходе из Layer 4 — кластеры очищаются

---
