# 🎮 Руководство: Генератор Уровней

## Обзор

Скрипт `generate_complex_levels.py` — это инструмент для автоматической генерации сложных уровней для **The Symmetry Vaults**.

---

## 🚀 Быстрый старт

### Запуск генератора

```bash
cd C:\Cursor\TayfaProject\AndroidGame
python generate_complex_levels.py
```

Это создаст 3 примера уровней:
- **level_13.json** — Тетраэдр (S₄, 24 ключа)
- **level_14.json** — Радужный хаос (D₄, 8 ключей, 6 цветов)
- **level_15.json** — Знакопеременная группа (A₄, 12 ключей)

---

## 📋 Доступные опции

### Кристаллы (nodes)

| Параметр | Опции | Описание |
|----------|-------|----------|
| **color** | `"red"`, `"blue"`, `"green"`, `"yellow"`, `"purple"`, `"orange"`, `"cyan"`, `"pink"`, `"white"`, `"black"` | Цвет кристалла |
| **position** | `[x, y]` | Координаты на экране (x: 200-900, y: 150-650) |
| **label** | `"A"`, `"B"`, `"C"`, ... | Буква на кристалле (автоматически) |

### Рёбра (edges)

| Параметр | Опции | Описание |
|----------|-------|----------|
| **type** | `"standard"` | Обычная тонкая линия |
|  | `"glowing"` | Светящаяся линия (для симметрии) |
|  | `"thick"` | Толстая линия (для выделения) |
|  | `"dashed"` | Пунктирная линия |
| **weight** | `1` | Всегда 1 (для будущего направленных графов) |

---

## 🔧 Создание своих уровней

### Пример 1: Большой структурированный уровень (S₄, S₅, A₄)

```python
from generate_complex_levels import LevelGenerator

# Создать генератор
gen = LevelGenerator("act1_level16", 16, act=1)

# Создать уровень с группой S₄ и тетраэдром
level = gen.create_big_structured_level(
    group_type="S4",        # Варианты: "S4", "S5", "A4", "D6"
    graph_type="tetrahedron",  # Варианты: "tetrahedron", "cube", "octahedron", "complete"
    title="Мой тетраэдр",
    subtitle="Сложный уровень"
)

# Сохранить
import json
with open("TheSymmetryVaults/data/levels/act1/level_16.json", 'w', encoding='utf-8') as f:
    json.dump(level, f, ensure_ascii=False, indent=2)
```

#### Доступные группы:
- **S4** — симметрическая группа порядка 24
- **A4** — знакопеременная группа порядка 12
- **S5** — симметрическая группа порядка 120 (очень большая!)
- **D6** — диэдральная группа порядка 12

#### Доступные графы:
- **tetrahedron** — тетраэдр (4 вершины, 6 рёбер)
- **cube** — куб (8 вершин, 12 рёбер)
- **octahedron** — октаэдр (6 вершин, 12 рёбер)
- **complete** — полный граф K₄

---

### Пример 2: Хаотичный уровень (много цветов)

```python
gen = LevelGenerator("act1_level17", 17, act=1)

# Хаотичный уровень
level = gen.create_chaotic_level(
    num_nodes=8,          # Количество узлов (не используется напрямую, зависит от group_type)
    num_colors=6,         # Количество различных цветов
    edge_variety=True,    # Использовать разные типы рёбер (thick, glowing, etc.)
    group_type="D4",      # Варианты: "D4", "S3"
    title="Радужный хаос",
    subtitle="Цвета запутывают"
)

# Сохранить
with open("TheSymmetryVaults/data/levels/act1/level_17.json", 'w', encoding='utf-8') as f:
    json.dump(level, f, ensure_ascii=False, indent=2)
```

#### Особенности хаотичных уровней:
- **Повторяющиеся цвета** — несколько кристаллов одного цвета (сложнее визуально отличить)
- **Разные типы рёбер** — толстые, тонкие, светящиеся
- **Сложная топология** — не очевидная структура графа

---

## 🎨 Примеры использования

### Создать уровень с кубом (S₄)

```python
gen = LevelGenerator("act1_level18", 18)
level = gen.create_big_structured_level(
    group_type="S4",
    graph_type="cube",
    title="Кубический зал",
    subtitle="Восемь вершин, двадцать четыре ключа"
)
```

### Создать уровень с октаэдром (A₄)

```python
gen = LevelGenerator("act1_level19", 19)
level = gen.create_big_structured_level(
    group_type="A4",
    graph_type="octahedron",
    title="Октаэдральный зал",
    subtitle="Только четные перестановки"
)
```

### Создать уровень с S₃ (два треугольника)

```python
gen = LevelGenerator("act1_level20", 20)
level = gen.create_chaotic_level(
    num_colors=3,
    edge_variety=True,
    group_type="S3",
    title="Два треугольника",
    subtitle="Одинаковые цвета"
)
```

---

## 🛠️ Продвинутое использование

### Добавить свою группу

Вы можете добавить собственную функцию генерации группы:

```python
def generate_my_custom_group(n: int) -> List[Dict]:
    """Генерирует вашу группу"""
    automorphisms = []

    # Ваша логика генерации автоморфизмов
    # ...

    return automorphisms
```

### Добавить свой граф

Вы можете добавить собственный метод для создания графа:

```python
class LevelGenerator:
    def _create_my_custom_graph(self):
        """Создать свой граф"""
        # Добавить узлы
        self.nodes = [
            {"id": 0, "color": "red", "position": [640, 300], "label": "A"},
            {"id": 1, "color": "blue", "position": [700, 400], "label": "B"},
            # ...
        ]

        # Добавить рёбра
        self.edges = [
            {"from": 0, "to": 1, "type": "glowing", "weight": 1},
            # ...
        ]
```

---

## 📊 Математические группы

### Симметрическая группа S_n
- **Порядок**: n!
- **Описание**: Все перестановки n элементов
- **Примеры**: S₃ = 6, S₄ = 24, S₅ = 120

### Знакопеременная группа A_n
- **Порядок**: n!/2
- **Описание**: Четные перестановки n элементов
- **Примеры**: A₃ = 3, A₄ = 12, A₅ = 60

### Диэдральная группа D_n
- **Порядок**: 2n
- **Описание**: Симметрии правильного n-угольника (повороты + отражения)
- **Примеры**: D₃ = 6, D₄ = 8, D₆ = 12

### Циклическая группа Z_n
- **Порядок**: n
- **Описание**: Только повороты правильного n-угольника
- **Примеры**: Z₃ = 3, Z₄ = 4, Z₆ = 6

---

## ⚠️ Ограничения

1. **Автоматическая генерация таблицы Кэли** пока не реализована (поле `cayley_table` пустое)
2. **Автоопределение генераторов** не реализовано (поле `generators` пустое)
3. **Подсказки** (hints) добавляются автоматически только базовые — лучше редактировать вручную в JSON

---

## 🐛 Решение проблем

### Уровень не загружается в игре

**Проблема**: Игра не видит новый уровень

**Решение**:
1. Проверьте, что файл лежит в правильной папке: `TheSymmetryVaults/data/levels/act1/`
2. Проверьте валидность JSON (можно через онлайн-валидатор)
3. Убедитесь, что `group_order` совпадает с количеством автоморфизмов

### Слишком много автоморфизмов

**Проблема**: S₅ генерирует 120 элементов, игра тормозит

**Решение**:
- Используйте меньшие группы (S₄, A₄, D₆)
- Для больших групп используйте подгруппы

### Цвета не те, что нужны

**Проблема**: Хочу свои цвета

**Решение**:
Отредактируйте список `COLORS` в начале скрипта:

```python
COLORS = ["red", "blue", "green", "yellow", "purple", "orange", "cyan", "pink", "white", "black"]
```

---

## 📚 Дополнительные ресурсы

- **Visual Group Theory** (Nathan Carter) — отличная книга про визуализацию групп
- **Abstract Algebra** (Hungerford) — для глубокого понимания теории групп
- **SymPy** (Python библиотека) — для работы с группами программно

---

## 🎯 Следующие шаги

1. **Запустите генератор** и создайте 3 примера уровней
2. **Откройте игру** и протестируйте новые уровни
3. **Отредактируйте JSON** — добавьте подсказки, улучшите названия
4. **Создайте свои уровни** — экспериментируйте с группами и графами
5. **Поделитесь результатами** — покажите, что получилось!

---

**Удачи в создании уровней!** 🚀
